{
    "name": "gen-com-ai",
    "targets": {
      "serve": {
        "executor": "nx:run-commands",
        "options": {
          "command": "uvicorn apps.gen-com-ai.app.main:app --host 0.0.0.0 --port 8001 --reload"
        }
      },
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "echo 'FastAPI does not require a build step'"
        }
      }
    }
  }
  